interface:
  display_name: "Skillshare"
  short_description: "Manage shared skills across AI clients"
  default_prompt: "Use $skillshare to inspect or perform the requested Skillshare operation."
policy:
  allow_implicit_invocation: true
