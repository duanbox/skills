interface:
  display_name: "EndGods Grill"
  short_description: "Explicit deep pressure-test for EndGods plans"
  default_prompt: "Use $grill-me to deeply challenge this EndGods plan and produce a decision record before implementation."
policy:
  allow_implicit_invocation: false
