interface:
  display_name: "Game UI Designer"
  short_description: "Resolve EndGods player-experience UI decisions"
  default_prompt: "Use $game-ui-designer when EndGods UI hierarchy, flow, navigation, accessibility, feedback, or player-visible states remain unresolved; do not use it as a prerequisite for an approved implementation."
